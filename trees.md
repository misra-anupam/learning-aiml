# Trees

In-order traversal: Left -> Center -> Right ==> Generates ordered nodes
Pre-order traversal: Center -> Left -> Right ==> Preserves tree structure
Post-order traversal: Left -> Right -> Center ==> Useful for cleanup, deletion, subdirectories where children are important